# PocketBase Control Plane Configuration
# Copy to /etc/pocketbase/control-plane.env and customize

# Node identification (unique per control plane instance)
NODE_ID=cp-1

# Raft cluster configuration
# Address this node binds to for Raft consensus
RAFT_BIND_ADDR=10.0.0.1:7000

# Address advertised to other nodes (use public IP if across networks)
RAFT_ADVERTISE_ADDR=10.0.0.1:7000

# Comma-separated addresses of existing Raft cluster members
# Leave empty for first node (bootstrap), set for joining nodes
# Example: RAFT_JOIN=10.0.0.1:7000,10.0.0.2:7000
RAFT_JOIN=

# Optional: Enable debug logging
# PB_LOG_LEVEL=DEBUG
